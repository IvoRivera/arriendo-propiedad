---
phase: 14
plan: 1
wave: 1
---

# Plan 14.1: Typography Foundation & Hero Unification

## Objective
Establish the core typography rules in `globals.css` and apply them to the Hero section to set the luxury tone for the entire site.

## Context
- .gsd/ROADMAP.md
- src/app/globals.css
- src/components/coastal/CoastalHero.tsx
- User Feedback: Two-font system (Newsreader for titles, Inter for everything else), specific tracking for CAPS.

## Tasks

<task type="auto">
  <name>Global Typography Tokens & Cleanup</name>
  <files>src/app/globals.css</files>
  <action>
    - Ensure H1 and H2 are strictly defined as font-serif (Newsreader).
    - Create a `.tracking-luxury` utility class for slightly wider letter-spacing on uppercase text (tracking-widest equivalent or custom 0.05em/0.1em).
    - Ensure font-sans (Inter) is the default for body, buttons, and inputs.
    - Remove any conflicting typography overrides in the global scope.
  </action>
  <verify>Check globals.css for the new utility classes and tag definitions.</verify>
  <done>
    - H1 and H2 use Newsreader.
    - Body uses Inter.
    - .tracking-luxury class exists.
  </done>
</task>

<task type="auto">
  <name>Hero Section Typography Unification</name>
  <files>src/components/coastal/CoastalHero.tsx</files>
  <action>
    - Set headline (H1) to font-serif (Newsreader).
    - Set tagline/subtitle to font-sans (Inter) with regular weight.
    - Standardize CTA buttons to use font-sans (Inter) with bold/medium weight and luxury tracking if uppercase.
    - Remove any hardcoded style overrides (style={{fontFamily: ...}}) and use Tailwind classes.
  </action>
  <verify>Visual check of the Hero section in the browser (if possible) or code review for class consistency.</verify>
  <done>
    - Hero uses maximum 2 font families.
    - Headlines are Serif.
    - Buttons and subtexts are Sans.
  </done>
</task>

## Success Criteria
- [ ] Global CSS defines the two-font system clearly.
- [ ] Hero section adheres strictly to the typography discipline.
- [ ] "Explorar disponibilidad" button uses the unified Sans font.
