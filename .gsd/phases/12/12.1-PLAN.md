---
phase: 12
plan: 1
wave: 1
---

# Plan 12.1: Limit Narrative Stack to 3 Images

## Objective
Reduce the number of initially displayed featured images in the Editorial Gallery from 5 to 3 to improve page scannability and content discovery speed.

## Context
- `src/components/coastal/EditorialGallery.tsx`

## Tasks

<task type="auto">
  <name>Update Image Slice and CTA Logic</name>
  <files>
    <file>src/components/coastal/EditorialGallery.tsx</file>
  </files>
  <action>
    - Change `displayImages` slice from `0, 5` to `0, 3`.
    - Update all logic conditions that use `images.length > 5` to `images.length > 3`.
    - Ensure the "Ver más" counter correctly shows `images.length - 3`.
    - Verify that the fade-out effect and discrete overlay appear on the 3rd image (index 2) when additional photos exist.
  </action>
  <verify>
    - Inspect the Editorial Gallery.
    - Confirm only 3 images are shown in the main stack.
    - Confirm the CTA appears on the 3rd image.
  </verify>
  <done>
    - Narrative stack is limited to 3 images.
    - "Ver más" functionality is updated and correct.
  </done>
</task>

## Success Criteria
- [ ] Exactly 3 images are shown in the initial editorial layout.
- [ ] Progressive Revelation CTA works correctly for the 3rd image.
- [ ] Image counter reflects the correct amount of hidden photos.
