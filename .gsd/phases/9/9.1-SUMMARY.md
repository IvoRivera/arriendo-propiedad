# Plan 9.1 Summary: Hub Foundation & Dual-Mode UI

Implemented the foundation for a dual-mode booking funnel in the availability section.

## Changes:
- **src/components/coastal/CoastalAvailability.tsx**:
    - Added `BookingIntent` type and `activeTab` state.
    - Implemented a premium Segmented Control (Pill style) with Framer Motion transitions.
    - Created the consultative view for long-stay leads with dedicated copy and CTA.
    - Added "Smart Upsell" logic that suggests long-stay mode when users select 21+ nights in the standard calendar.
- **src/app/home-client.tsx**:
    - Updated `openModal` and `CoastalAvailability` calls to pass the `BookingIntent` mode.
- **src/components/coastal/CoastalRequestModal.tsx**:
    - Added `intentMode` prop to prepare for the expanded form.

## Verification:
- Manual check of the UI shows smooth transitions between "Reserva por fechas" and "Estadía prolongada".
- Selecting a long range (21+ nights) correctly triggers the upsell banner.
- Both "Confirmar" and "Solicitar propuesta" CTAs correctly trigger the modal.
