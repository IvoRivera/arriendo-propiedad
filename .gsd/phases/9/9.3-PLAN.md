---
phase: 9
plan: 3
wave: 3
depends_on: ["9.2"]
files_modified: ["src/components/coastal/CoastalRequestModal.tsx", "src/components/coastal/CoastalAvailability.tsx", "src/app/api/public/bookings/route.ts"]
autonomous: true

must_haves:
  truths:
    - "Long-stay leads are formatted and sent to the backend correctly"
    - "Transitions between modes are smooth and cinematic"
  artifacts:
    - "src/app/api/public/bookings/route.ts handles or formats long-stay data"
---

# Plan 9.3: Integration & UX Polish

<objective>
Finalize the integration by ensuring data is correctly formatted for the backend and polishing the transitions and responsiveness for a high-end feel.
</objective>

<context>
- src/components/coastal/CoastalRequestModal.tsx
- src/components/coastal/CoastalAvailability.tsx
- src/app/api/public/bookings/route.ts
</context>

<tasks>

<task type="auto">
  <name>Payload Formatting and Backend Integration</name>
  <files>src/components/coastal/CoastalRequestModal.tsx, src/app/api/public/bookings/route.ts</files>
  <action>
    - In `onSubmit`, if mode is `long-stay`, format the extra fields (Duration, Type, Budget, Amenities) into a clean, structured string.
    - Prepend `[LONG STAY LEAD]` to the `trip_reason` field to ensure visibility for the admin.
    - (Optional) Check if `route.ts` needs logic to handle this specific prefix for notification subjects.
  </action>
  <verify>Submission sends a formatted trip_reason string with all long-stay details.</verify>
  <done>Long-stay data reaches the backend without breaking the current schema.</done>
</task>

<task type="auto">
  <name>Cinematic Transitions and Responsive Polish</name>
  <files>src/components/coastal/CoastalAvailability.tsx, src/components/coastal/CoastalRequestModal.tsx</files>
  <action>
    - Add `AnimatePresence` and `motion.div` to the Availability section content to handle switching between Calendar and Consultative view.
    - Ensure `min-h` is set to prevent layout jumps during transitions.
    - Review Mobile UX: Ensure the expanded modal is fully scrollable and the bottom CTA is always accessible.
    - Apply final color refinements (#00628f, #9a8a78) to match the brand identity.
  </action>
  <verify>Switching modes has a smooth fade/slide effect. Modal is scrollable on mobile.</verify>
  <done>The entire flow feels premium and works flawlessly on mobile.</done>
</task>

</tasks>

<verification>
- [ ] Backend receives all new fields in the formatted reason string
- [ ] Transitions are smooth without layout shifts
- [ ] Mobile UX is verified (scrolling, padding)
</verification>

<success_criteria>
- [ ] Long-stay feature is fully integrated and polished
- [ ] Project maintains high performance and clean architecture
</success_criteria>
