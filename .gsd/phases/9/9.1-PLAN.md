---
phase: 9
plan: 1
wave: 1
depends_on: []
files_modified: ["src/components/coastal/CoastalAvailability.tsx"]
autonomous: true

must_haves:
  truths:
    - "User can switch between 'Reserva por fechas' and 'Estadía prolongada' modes"
    - "Selecting 21+ nights in standard mode triggers a long-stay suggestion"
  artifacts:
    - "src/components/coastal/CoastalAvailability.tsx contains mode state and Segmented Control UI"
---

# Plan 9.1: Hub Foundation & Dual-Mode UI

<objective>
Transform the Availability section into a dual-mode hub that supports both standard date-based bookings and a new long-stay consultative flow.
</objective>

<context>
- src/components/coastal/CoastalAvailability.tsx
- src/config/site-content.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Dual-Mode State and Segmented Control</name>
  <files>src/components/coastal/CoastalAvailability.tsx</files>
  <action>
    - Define type `BookingIntent = 'standard' | 'long-stay'`.
    - Add `activeTab` state with 'standard' as default.
    - Implement a minimalist "Pill" styled Segmented Control at the top of the section (bg-white/50, backdrop-blur).
    - Use Framer Motion for the active tab highlight transition (layoutId).
  </action>
  <verify>Visual check: Segmented control appears and toggles state.</verify>
  <done>Tabs are functional and match the premium design system.</done>
</task>

<task type="auto">
  <name>Implement Consultative View for Long Stay</name>
  <files>src/components/coastal/CoastalAvailability.tsx</files>
  <action>
    - When `activeTab === 'long-stay'`, hide the DayPicker.
    - Render the consultative UI:
      - Title: "Algunas estadías merecen otro ritmo."
      - Subtitle: "Si planeas una reserva larga o teletrabajo por temporada, podemos preparar una propuesta personalizada."
      - Primary CTA: "Solicitar propuesta personalizada".
    - Update `handleContinue` to emit the mode to the parent.
  </action>
  <verify>Switching to 'Estadía prolongada' shows the consultative text and new button.</verify>
  <done>Long-stay intro is visually consistent and triggers the correct action.</done>
</task>

<task type="auto">
  <name>Smart Upsell Logic (21+ Nights)</name>
  <files>src/components/coastal/CoastalAvailability.tsx</files>
  <action>
    - In standard mode, detect if selected `nights >= 21`.
    - Render a subtle notification/banner under the price summary: "¿Planeas una estadía extensa? Cambiar a modo larga estadía".
    - Clicking the banner switches `activeTab` to 'long-stay'.
  </action>
  <verify>Selecting 21+ nights in the calendar triggers the suggestion banner.</verify>
  <done>Upsell banner appears naturally and is functional.</done>
</task>

</tasks>

<verification>
- [ ] Tabs switch between modes smoothly
- [ ] Long stay view has correct copy and CTA
- [ ] 21+ nights trigger the upsell suggestion
</verification>

<success_criteria>
- [ ] Dual-mode UI is integrated into CoastalAvailability
- [ ] No regression in standard booking flow
</success_criteria>
