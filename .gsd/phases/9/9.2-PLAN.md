---
phase: 9
plan: 2
wave: 2
depends_on: ["9.1"]
files_modified: ["src/components/coastal/CoastalRequestModal.tsx", "src/app/home-client.tsx"]
autonomous: true

must_haves:
  truths:
    - "Modal correctly renders new fields in 'long-stay' mode"
    - "Zod validation allows optional fields in standard mode and requires them in long-stay"
  artifacts:
    - "src/components/coastal/CoastalRequestModal.tsx expanded with consultative fields"
---

# Plan 9.2: Consultative Request Flow & Validations

<objective>
Expand the request modal to accommodate the detailed consultative form required for long-stay leads, ensuring a premium UX with custom interactive elements.
</objective>

<context>
- src/components/coastal/CoastalRequestModal.tsx
- src/app/home-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Bridge Intent Mode from Hub to Modal</name>
  <files>src/app/home-client.tsx, src/components/coastal/CoastalRequestModal.tsx</files>
  <action>
    - Update `home-client.tsx` to track `bookingIntent` and pass it to `CoastalRequestModal`.
    - Update `CoastalRequestModal` props to accept `intentMode: BookingIntent`.
    - Ensure the modal resets its internal state when switching between modes.
  </action>
  <verify>Opening the modal from different tabs shows different header/title.</verify>
  <done>Intent mode is correctly propagated from the availability section to the form.</done>
</task>

<task type="auto">
  <name>Expand Zod Schema and Form Fields</name>
  <files>src/components/coastal/CoastalRequestModal.tsx</files>
  <action>
    - Refactor `requestSchema` to use `z.discriminatedUnion` based on `intentMode` or separate schemas.
    - Implement UI for new fields in 'long-stay' mode:
      - Approximate Start Date (simple date picker).
      - Flexible Dates checkbox.
      - Duration Select (2-4 wks, 1 mo, 2 mos, Flexible).
      - Stay Type Select (Teletrabajo, Vacaciones, Mudanza, Trabajo temporal, Otro).
      - Budget (optional) with helper text.
  </action>
  <verify>Form fields appear and validate correctly for both modes.</verify>
  <done>Long-stay fields are integrated and respect validation rules.</done>
</task>

<task type="auto">
  <name>Implement 'Token/Chip' Multi-select for Amenities</name>
  <files>src/components/coastal/CoastalRequestModal.tsx</files>
  <action>
    - Create a custom interactive grid for "Necesidades importantes" (Amenities).
    - Use chip-style buttons instead of standard checkboxes.
    - Active state: `#00628f` background, white text.
    - Inactive state: Tenue border/background.
    - Amenities: Buen internet, Escritorio / trabajo remoto, Estacionamiento, Cocina equipada, Lavadora, Flexibilidad check-in/out.
  </action>
  <verify>Clicking a chip toggles its state and updates the form value.</verify>
  <done>Amenities selection feels tactile and premium.</done>
</task>

</tasks>

<verification>
- [ ] Modal header changes based on intent
- [ ] Long stay fields are required/validated correctly
- [ ] Amenities chips work as expected
</verification>

<success_criteria>
- [ ] Comprehensive long-stay form is functional
- [ ] Standard form remains clean and unchanged
</success_criteria>
