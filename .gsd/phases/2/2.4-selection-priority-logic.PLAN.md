---
phase: 2.2
plan: 2.4
wave: 2
---

# Plan 2.4: Selection & Priority Logic

## Objective
Implement interactive day selection, custom color usage, and priority-based display in the Pricing Calendar.

## Context
- src/components/admin/PricingCalendar.tsx
- src/components/admin/PricingManager.tsx
- src/components/admin/RuleForm.tsx
- src/components/admin/SeasonTable.tsx

## Tasks

<task type="auto">
  <name>Implement Color Picker and Form Support</name>
  <files>
    src/components/admin/RuleForm.tsx
    src/components/admin/SeasonTable.tsx
    src/components/admin/PricingManager.tsx
  </files>
  <action>
    - Update `RuleForm` to include a color input for the `color_hex` field.
    - Update `SeasonTable` to display the rule's color and allow editing it.
    - Update `PricingManager` handlers (`handleAddRule`, `handleUpdateRule`) to include `color_hex`.
  </action>
  <verify>Check that RuleForm has a color input and SeasonTable shows the selected color.</verify>
  <done>Color field is editable and persisted for each rule.</done>
</task>

<task type="auto">
  <name>Enhance Calendar Interaction and Logic</name>
  <files>src/components/admin/PricingCalendar.tsx</files>
  <action>
    - Add an `onDateSelect` prop to `PricingCalendar`.
    - Implement a click handler on calendar days that calls `onDateSelect(date)`.
    - Refine `getDayDetails`:
        - Ensure it uses the rule's `color_hex` for highlighting.
        - Ensure it only shows the `source` (rule name) of the highest priority rule.
    - In `PricingManager`, use `onDateSelect` to pre-fill the `RuleForm` start/end dates.
  </action>
  <verify>Clicking a day fills the form; calendar shows custom colors and handles overlaps correctly.</verify>
  <done>Interactive selection and priority-based display implemented.</done>
</task>

## Success Criteria
- [ ] Calendar days are selectable to create/edit rules.
- [ ] Overlapping rules are handled visually by priority.
- [ ] Custom colors are used for each rule's visual representation.
