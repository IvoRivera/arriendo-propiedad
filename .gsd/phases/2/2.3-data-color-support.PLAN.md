---
phase: 2.2
plan: 2.3
wave: 1
---

# Plan 2.3: Data Layer & Color Support

## Objective
Extend the database and types to support custom colors for each seasonal pricing rule.

## Context
- .gsd/ROADMAP.md
- src/types/pricing.ts
- Supabase project gbelcihimvuodnpjwelf

## Tasks

<task type="auto">
  <name>Add color_hex column to Supabase</name>
  <files>database</files>
  <action>
    Execute SQL to add a `color_hex` column (TEXT) to the `seasonal_pricing` table.
    - Default value could be a soft primary tint like '#00628f1a' (alpha included if supported by picker, otherwise plain hex).
    - Let's use plain HEX for better compatibility: '#00628f'.
  </action>
  <verify>SELECT column_name FROM information_schema.columns WHERE table_name = 'seasonal_pricing' AND column_name = 'color_hex';</verify>
  <done>Column 'color_hex' exists in 'seasonal_pricing' table.</done>
</task>

<task type="auto">
  <name>Update types and schemas</name>
  <files>src/types/pricing.ts</files>
  <action>
    - Add `color_hex?: string` to `SeasonalPricing` interface.
    - Update `PricingUpdateSchema` (Zod) to include `color_hex` as an optional string.
  </action>
  <verify>Check types compilation.</verify>
  <done>SeasonalPricing and PricingUpdateSchema include color_hex.</done>
</task>

## Success Criteria
- [ ] Database supports rule colors.
- [ ] TypeScript types reflect the new field.
