---
phase: 11
plan: 2
wave: 1
---

# Plan 11.2: Progressive Revelation Gallery Redesign

## Objective
Implement a "Progressive Revelation" climax for the Editorial Gallery, integrating the "See More" invitation into the last featured image and removing heavy UI blocks.

## Context
- `src/components/coastal/EditorialGallery.tsx`
- `.gsd/phases/11/DEBUG.md`

## Tasks

<task type="auto">
  <name>Integrate CTA into Last Image</name>
  <files>
    <file>src/components/coastal/EditorialGallery.tsx</file>
  </files>
  <action>
    - Refactor the image loop in `EditorialGallery.tsx`.
    - For the last image (index 4) when more than 5 images exist:
      - Enhance the `mask-fade-bottom` effect with a linear gradient to the background color.
      - Add a centered or bottom-right subtle overlay *inside* the image container.
      - Overlay specs: Small glassmorphism circle, thin `Plus` icon, and minimalist text "VER GALERÍA COMPLETA [+n]" (text-[10px], Deep Navy color).
  </action>
  <verify>
    - Inspect the last image of the narrative stack.
    - Verify the CTA is subtly integrated into the image area.
  </verify>
  <done>
    - CTA is integrated into the last image.
    - Minimalist typography and icon are used.
  </done>
</task>

<task type="auto">
  <name>Remove Heavy UI Blocks and Adjust Spacing</name>
  <files>
    <file>src/components/coastal/EditorialGallery.tsx</file>
  </files>
  <action>
    - Delete the "Immersive Gallery Climax" block (lines 89-117).
    - Reduce the vertical padding of the section to `py-10` or similar to allow fluid transition to the next section.
    - Ensure the transition to carrusels is seamless.
  </action>
  <verify>
    - Verify the large button block is gone.
    - Inspect the spacing at the bottom of the section.
  </verify>
  <done>
    - Large CTA block is removed.
    - Spacing is adjusted for a fluid scroll flow.
  </done>
</task>

## Success Criteria
- [ ] "See More" invitation is elegant, discrete, and integrated into the last photo.
- [ ] Large UI blocks that compete with photos are eliminated.
- [ ] Scroll flow towards "Recorrido" and "Amenidades" is uninterrupted.
