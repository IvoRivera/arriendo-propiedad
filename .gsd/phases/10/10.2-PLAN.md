---
phase: 10
plan: 2
wave: 1
---

# Plan 10.2: Typography Unification & Contrast

## Objective
Unify typography across the site according to the Design System and improve legibility of secondary labels and editorial text.

## Context
- `src/components/coastal/EditorialGallery.tsx`
- `docs/design/DESIGN.md`
- `src/app/layout.tsx`

## Tasks

<task type="auto">
  <name>Editorial Gallery Legibility & Contrast</name>
  <files>
    <file>src/components/coastal/EditorialGallery.tsx</file>
  </files>
  <action>
    - Increase font size for "Momento {n}" labels (e.g., from `text-[10px]` to `text-xs`).
    - Remove `opacity-60` from these labels and the "Exploración Completa" text to improve contrast.
    - Change font from `mono` to `sans` (Inter) for these labels to align with the design system's body/functional font rules.
  </action>
  <verify>
    - Inspect the "Momento" labels in the gallery; they should be larger and fully opaque.
    - Inspect "Exploración Completa"; it should be higher contrast and use the Inter font.
  </verify>
  <done>
    - Secondary labels in EditorialGallery are legible and high-contrast.
    - Labels use the Inter font instead of monospace.
  </done>
</task>

<task type="auto">
  <name>Typography Uniformity Audit</name>
  <files>
    <file>src/components/coastal/*.tsx</file>
  </files>
  <action>
    - Search for usages of `font-mono` across the components (excluding cases where it might be absolutely necessary, though the user requested uniformity).
    - Replace `font-mono` with `font-sans` (Inter) and use `tracking-widest` and `uppercase` where a "label" feel is desired.
    - Ensure all section titles consistently use the Newsreader serif font (`font-serif`) and functional text uses Inter (`font-sans`).
  </action>
  <verify>
    - Run a grep for `font-mono` and ensure it's removed from UI components.
    - Verify that the overall site typography feels cohesive and follows DESIGN.md.
  </verify>
  <done>
    - `font-mono` is eliminated from standard UI elements.
    - Site typography is unified (Newsreader for titles/editorial, Inter for body/labels).
  </done>
</task>

## Success Criteria
- [ ] Gallery labels are easy to read and high-contrast.
- [ ] Typography is uniform across the site (excluding the hero as requested).
- [ ] Monospace font is removed in favor of Inter for a premium look.
