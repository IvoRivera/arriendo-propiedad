---
phase: 10
plan: 1
wave: 1
---

# Plan 10.1: Visual Refinement & Mobile Optimization

## Objective
Address specific layout issues in the Hero, Gallery, and Discover sections to improve mobile experience and visual balance.

## Context
- `src/components/coastal/CoastalHero.tsx`
- `src/components/coastal/EditorialGallery.tsx`
- `src/components/coastal/CoastalDiscover.tsx`
- `src/config/site-content.ts`

## Tasks

<task type="auto">
  <name>CoastalHero Mobile Eyebrow Refinement</name>
  <files>
    <file>src/components/coastal/CoastalHero.tsx</file>
  </files>
  <action>
    - Modify the eyebrow section in `CoastalHero.tsx`.
    - Use CSS or conditional rendering to hide the separator dots "·" if they would appear at a line break.
    - Limit the eyebrow content to a maximum of 2 lines on mobile view using `line-clamp-2` or a similar technique.
    - Ensure the layout remains balanced and legible on small screens.
  </action>
  <verify>
    - Inspect the component in mobile view (viewport < 640px).
    - Verify that no separator dot "·" is orphaned or misplaced at line breaks.
    - Verify that the eyebrow text does not exceed 2 lines on mobile.
  </verify>
  <done>
    - Eyebrow dots are only visible between items on the same line.
    - Eyebrow is restricted to 2 lines max on mobile.
  </done>
</task>

<task type="auto">
  <name>Gallery & Discover Alignment</name>
  <files>
    <file>src/components/coastal/EditorialGallery.tsx</file>
    <file>src/components/coastal/CoastalDiscover.tsx</file>
  </files>
  <action>
    - In `EditorialGallery.tsx`: Center the header (title and subtitle) for both mobile and desktop. Ensure `max-w-4xl` doesn't prevent centering.
    - In `CoastalDiscover.tsx`: Center the emojis and the item titles below.
  </action>
  <verify>
    - Inspect the Gallery section; title and subtitle should be centered horizontally.
    - Inspect the Discover section; emojis should be inline or vertically aligned with titles.
  </verify>
  <done>
    - EditorialGallery header is centered.
    - CoastalDiscover emojis are inline with titles.
  </done>
</task>

## Success Criteria
- [ ] CoastalHero eyebrow is clean and compact (2 lines max) on mobile.
- [ ] Gallery headers are centered on all devices.
- [ ] Discover emojis are properly integrated with titles.
