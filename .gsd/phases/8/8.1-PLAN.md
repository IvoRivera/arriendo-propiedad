---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Immersive Lightbox Scaling & Mobile Optimization

## Objective
Enhance the gallery lightbox experience on mobile devices to ensure images occupy the maximum screen real estate ("edge-to-edge") while maintaining a high-end, elegant aesthetic.

## Context
- .gsd/SPEC.md
- src/components/coastal/Lightbox.tsx
- src/components/coastal/CoastalGallery.tsx

## Tasks

<task type="auto">
  <name>Optimize Lightbox Mobile Layout</name>
  <files>
    <file>src/components/coastal/Lightbox.tsx</file>
  </files>
  <action>
    - Remove `p-4` padding on mobile from the main container and image wrapper to allow edge-to-edge display.
    - Adjust `max-w-6xl` to `max-w-none` on mobile to ensure no width capping.
    - Reposition the "Top Bar" (Counter and Close button) to be more compact on mobile (e.g., smaller padding, smaller icons).
    - Refine the "Bottom Caption" and "Swipe Hint" to minimize intrusion on the image area.
    - Ensure `object-contain` is still used as the base, but verify if the container allows the image to touch the screen edges if the aspect ratio allows.
  </action>
  <verify>Visual inspection (or simulate mobile viewport in browser if possible, otherwise rely on code logic).</verify>
  <done>Images in the lightbox touch the screen edges on mobile (zero margin) and the UI elements are non-obstructive.</done>
</task>

<task type="auto">
  <name>Enhance Lightbox Aesthetics & Interactivity</name>
  <files>
    <file>src/components/coastal/Lightbox.tsx</file>
  </files>
  <action>
    - Add a subtle background gradient or increase `backdrop-blur` for a more premium "glass" feel.
    - Implement a "Double Tap to Zoom" or simple scale transition if the user wants to see details (optional, but good for "not enlarging enough" feedback).
    - Ensure the swipe gesture logic remains solid with the new edge-to-edge layout.
  </action>
  <verify>Check Framer Motion animations and gesture handlers.</verify>
  <done>Lightbox feels more immersive and "premium" with improved scaling options on mobile.</done>
</task>

## Success Criteria
- [ ] Lightbox images on mobile have 0px margin to the screen edges.
- [ ] Close button and counter are usable but less intrusive on small screens.
- [ ] Transitions between images are smooth and high-performance.
