---
phase: 3
plan: 3.1
wave: 1
---

# Plan 3.1: Layout Architecture & Sidebar Integration

## Objective
Implement the new 2-column layout and the dynamic sidebar editor to eliminate scrolling and centralize pricing management.

## Context
- src/components/admin/PricingManager.tsx
- src/components/admin/PricingSidebar.tsx (New)
- src/components/admin/PricingHeader.tsx (New)
- src/components/admin/PricingTabs.tsx (New)

## Tasks

<task type="auto">
  <name>Create PricingSidebar component</name>
  <files>src/components/admin/PricingSidebar.tsx</files>
  <action>
    Develop a sticky sidebar component that:
    - Accepts `mode` ('create' | 'edit' | 'idle').
    - Displays form fields (Season name, Dates, Price, Weekend Price, Category, Priority).
    - Includes "Save", "Cancel", and "Delete" (in edit mode) buttons.
    - Uses the predefined `PRICING_COLORS`.
  </action>
  <verify>Sidebar renders correctly in different modes.</verify>
  <done>PricingSidebar functional.</done>
</task>

<task type="auto">
  <name>Refactor PricingManager Layout</name>
  <files>src/components/admin/PricingManager.tsx</files>
  <action>
    - Change layout to a grid/flex container with 2 columns (70/30).
    - Implement `activeTab` state (Calendar, Rules, History).
    - Integrate `PricingHeader` at the top.
    - Move `RuleForm` logic into `PricingSidebar`.
    - Manage sidebar state (open/closed, mode, target data).
  </action>
  <verify>2-column layout visible without horizontal scroll; tabs switch correctly.</verify>
  <done>Layout refactored.</done>
</task>

<task type="auto">
  <name>Create PricingHeader component</name>
  <files>src/components/admin/PricingHeader.tsx</files>
  <action>
    - Show current base price.
    - Show quick status (Active rules count, next holiday).
    - Add "Quick Actions" dropdown.
  </action>
  <verify>Header shows accurate data from usePricingData.</verify>
  <done>PricingHeader functional.</done>
</task>

## Success Criteria
- [ ] UI is 2-column (Calendar left, Sidebar right).
- [ ] No more vertical scroll to find the creation form.
- [ ] Tabs allow switching between Calendar and Table views.
