---
phase: 3
plan: 3.2
wave: 2
---

# Plan 3.2: Advanced Calendar Interactions

## Objective
Enhance the Pricing Calendar with range selection, integrated holidays, and modern tooltips.

## Context
- src/components/admin/PricingCalendar.tsx
- src/lib/date-utils.ts

## Tasks

<task type="auto">
  <name>Implement Range Selection (Drag & Drop)</name>
  <files>src/components/admin/PricingCalendar.tsx</files>
  <action>
    - Add `onRangeSelect` callback.
    - Implement `mousedown`, `mouseenter`, and `mouseup` handlers to detect date ranges.
    - Visually highlight the "selection range" during drag.
    - On `mouseup`, trigger the sidebar "Create" mode with the selected range.
  </action>
  <verify>Can select multiple days by dragging; sidebar opens with correct dates.</verify>
  <done>Range selection implemented.</done>
</task>

<task type="auto">
  <name>Integrate Holiday Icons & Tooltips</name>
  <files>src/components/admin/PricingCalendar.tsx</files>
  <action>
    - Add holiday names to calendar day tooltips.
    - Use Lucide icons (e.g., `Gift` or `Star`) for holidays.
    - Add "Quick Create" button inside holiday cells.
  </action>
  <verify>Holidays are visually distinct and interactive.</verify>
  <done>Holiday integration complete.</done>
</task>

## Success Criteria
- [ ] Users can select a range of dates by dragging.
- [ ] Hovering over a day shows the applied price and rule name.
- [ ] Holidays are clearly marked and actionable.
